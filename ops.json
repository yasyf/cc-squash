{"v":1,"lamport":1,"ops":[{"kind":"create_doc","nonce":"acde496422d75d283322f10b56012ed1","title":"Layer 4 AS-BUILT: live cache-economics rewriter (Interceptor + retrieve)","body":"# Layer 4 AS-BUILT — the live cache-economics rewriter\n\nLayer 4 (\"Live engine\") shipped as ONE `feat` commit on `main` (a124c7d0 at write time), built straight through in 5 dependency-ordered sub-phases (4a–4e) via dynamic workflows. **260 Rust tests + the full Go suite green; clippy `-D warnings` clean; `/codex`-reviewed (no safety bugs).** Mock-proven end-to-end; the paid live A/B is the only deferred-to-user piece. Plan: `.claude/research/cc-squash-build-plan.md` §7 Layer 4. Prior layers: cc-notes `8d86c08`/`743d0b0`/`4006fbf`.\n\n## The control loop (the spine)\n- **L0 — sync, read-only, hot path.** `usage_tap.rs` wraps the upstream SSE stream (yield-before-scan, ≤64 KB bounded scanner) and reads `usage` from the FIRST `message_start` (`message.usage` carries `cache_creation/cache_read/input`; `message_delta` does not) → `CacheState::observe`. Never waits for stream end.\n- **L1 — async, off critical path.** After forward, `tokio::spawn(stage_next)` segments the ORIGINAL incoming body (CC resends its full transcript each turn), scores `is_squash_candidate`, calls the summarizer `decide`, `RefStore::put`s originals, and stages a `StagedPlan` keyed by **content-address** (`RefId`), then `fold`s `WorkingState`. EVERY `.await` is lock-free (clone out of the `Arc\u003cMutex\u003cSessionEcon\u003e\u003e` under a brief sync lock; re-lock only to commit).\n- **L2 — sync, one-bust-on-egress, hot path.** `intercept.rs` wedges into the `Decision::Forward` arm, wrapped in `catch_unwind` + `tokio::time::timeout(50ms)` + a 4 MiB size gate. It re-segments the LIVE body, re-hashes each segment via `segment_payload_bytes`+`content_address` to match the staged plan, recomputes geometry, runs `select_strategy` (re-runs NPV/pin/pre-gate — trusts ITS result, not the staged choice), RefHot-pre-filters, runs `Controller::decide`, and on Flush/RideFreeBust splices + gates. The hot path NEVER calls the LLM.\n\n## Cardinal invariant (verified by adversarial review + tests)\nFail-open to identity: ANY error/timeout/panic/gate-failure forwards the original request + streams the original response byte-for-byte. Every `intercept.rs` exit returns `identity(original)`; the only non-identity arms return gate-validated rewritten bytes.\n\n## Key design decisions / as-built facts\n- **Staging key = content-address, NOT MessageId.** `MessageId` is a positional index (`segment.rs:201`), so a `source_uuid` key breaks when the array grows. `ccs_policy::segment_payload_bytes(seg,\u0026body)` is the SOLE canonicalizer, byte-stable across grown bodies, used by both L1 (put) and L2 (match).\n- **The splice (`ccs-policy/src/rewrite.rs`) edits ORIGINAL bytes**, never rebuilds from `WireBody` (which models only 5 keys): top-level parsed as `BTreeMap\u003cString, Box\u003cRawValue\u003e\u003e`, only `messages` replaced; untouched messages returned as their raw `Box\u003cRawValue\u003e` span (thinking signatures byte-identical); all sibling envelope keys (metadata/temperature/tool_choice/stream/…) preserved. cache_control: STRIPS CC's existing message-level hints and re-applies our own plan; `effective_cap = 4 − (system+tools hints)`; suppression logged.\n- **The validity gate (`ccs-policy/src/rewrite_gate.rs`) is the fail-safe backstop** — 6 checks (tool-pair, role preservation, ≤4 cache_control, monotonic-shrink total+per-message, latest-thinking byte-identical, no envelope key dropped); any Err ⇒ forward original. Funnels BOTH the staged-plan path and the deterministic-fallback path.\n- **`RefStore::retrieve` is now session-scoped** (added `session_id` param → `WHERE ref_id=?1 AND session_id=?2`); a cross-session ref is an indistinguishable Miss.\n- **rmcp 1.8** (confirmed from source): `StreamableHttpService` with `.with_stateful_mode(false).with_json_response(true)`, `#[tool_router]`/`#[tool]`/`#[tool_handler]`; mounted on a SEPARATE 127.0.0.1 listener (own `CatchPanicLayer`) — NOT the relay Router (the `/s/*rest` demux catch-all would collide). axum 0.7 route is `/s/:token/mcp` (`:token`, not `{token}`).\n- **`hot_refs`** populated by the retrieve tool (access_count ≥ 2) → the 4d RefHot pre-filter has a live producer.\n- **GC** (`Gc` seam frame from `ccs gc`): reachable = union of all sessions' staged ref_ids; staged refs never evicted; GRACE=600s \u003e squash→persist latency.\n- **Breaker** trips on warmup-guarded all-zero OR realized≫predicted (factor 4×); `last_predicted_bust` stashed by L2, read by L0; disengage = per-session identity for a 60 s cooldown.\n- **npv_floor** wired at BOTH `controller.rs` (`\u003e self.npv_floor`) and `candidate.rs` (`\u003c= npv_floor`) reading `EconomicsConfig.npv_floor`; default 0.0 preserves all prior outcomes.\n- **Config seam:** Go is the sole `config.toml` parser (`go/internal/config`), pushes JSON over `mint{token,config}`; `RelayConfig` composes `EconomicsKnobs`/`PolicyKnobs` mirrors with `#[serde(default)]` + `From` impls.\n\n## Known conservatism / follow-ups\n- **The continuous loop currently squashes only single-message string-content segments** (`string_content_target`). Tool-pair / array-content segments fail-open (skipped) — so the biggest reclaimable bulk (tool-result output, which is array content) is NOT squashed yet. Safe + correct, but limits realized savings. Follow-up: target a tool_result's text block via `SegmentTarget::Block{msg,block}` (the splice already supports it; the gate already enforces tool-pair integrity).\n- **Deterministic fallback is lossy** (strip+drop only; no async `put` on the hot path) and fires only under `OverBudget` with no staged plan (turn 1 / L1 lag).\n- **`count_hints_raw` is a substring count** of `\"cache_control\"` — a literal in a system-prompt text value over-counts (safe-direction: fewer hints, never \u003e4); commented.\n\n## Deferred to the user (the live A/B — needs creds + interactive CC)\n1. **Retrieve-through-real-CC:** `ccs run` → confirm CC parses the injected `--mcp-config`, surfaces `cc_squash_retrieve`, and a placeholder's `retrieve(\"sha256:…\")` materializes the original back.\n2. **Exp D cache observability:** realized `cache_creation`/`cache_read` match the tail-cheap/head-expensive prediction; no thinking-block-mutation 400s.\n3. **AB-oracle:** three-arm paired live A/B (cc-squash vs CC-builtin vs no-compaction oracle), ≥10 seeds, paired McNemar/Wilcoxon BCa; breaker auto-reverts on forced over-bust. The full `ccs-eval` statistical substrate is not yet built (the plan's separate parallel strand). Runbook: see the companion cc-notes doc.\n\n## Files (new)\nRust: `ccs-proxy/src/{intercept,mcp,session,staging,usage_tap}.rs`, `ccs-policy/src/{rewrite,rewrite_gate,payload}.rs`. Go: `go/internal/config/config.go`. Plus seam/relay/forward/app/config/demux/main + Go cli/control/proxyseam edits.","when":"before working on Layer 5 (transcript durability mirror) or modifying the live engine (ccs-proxy intercept/mcp/session/staging/usage_tap, ccs-policy rewrite/rewrite_gate/payload); gives the L0/L1/L2 control loop, the fail-open invariant, the splice/gate contracts, rmcp+GC wiring, and the deferred live-A/B","tags":null,"anchors":[{"kind":"dir","value":"crates/"},{"kind":"dir","value":"go/"}]}]}